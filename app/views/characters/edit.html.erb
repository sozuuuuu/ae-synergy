<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <h1 class="text-4xl font-bold text-gray-900"><%= @character.name %>の編集</h1>
    <p class="text-gray-600 mt-2">キャラクター情報を更新します</p>
  </div>

  <%= render "form", character: @character %>
</div>
